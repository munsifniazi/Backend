const add = (a, b) => {
  return a + b;
};

const subtract = (a, b) => {
  return a - b;
};

const multiply = (a, b) => {
  return a * b;
};

const divide = (a, b) => {
  return a / b;
};
const PI = 3.14;
module.exports = {
  add,
  subtract,
  multiply,
  divide,
  PI,
}; // Exporting add function to index.js file

// Usage in another file (index.js)
