import { add, subtract, multiply, divide, PI } from './math.js';

console.log(add(51, 27));
console.log(subtract(51, 27));
console.log(multiply(51, 27));
console.log(divide(54, 27));

console.log(PI);

// Importing and using a function from another JavaScript file (math.js) in a JavaScript file (index.js) using ES6 modules